fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic-settings==2.5.2
sqlalchemy==2.0.35
pypdf==5.0.1
python-docx==1.2.0
python-pptx==1.0.2
python-multipart==0.0.12
# V3 Milestone 1 Phase 2: password hashing for account authentication
# (see app/services/auth_service.py). Used directly, not through
# passlib -- passlib is effectively unmaintained and has known
# compatibility issues with modern bcrypt releases; calling
# bcrypt.hashpw/checkpw directly is one dependency instead of two and
# needs no compatibility shim.
bcrypt==5.0.0
google-genai==2.11.0
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==12.1.1
# pytesseract and pdf2image above are Python wrappers only. OCR and
# scanned-PDF processing also require two OS-level binaries that pip
# cannot install, and that must be installed separately on the host:
#   - tesseract-ocr   (e.g. `apt install tesseract-ocr` / `brew install tesseract`)
#   - poppler-utils   (e.g. `apt install poppler-utils` / `brew install poppler`)
# See app/services/ocr/dependency_check.py — the backend logs a
# warning at startup if either is missing.
